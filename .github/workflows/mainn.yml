name: Build Unsigned IPA
on: [workflow_dispatch]

jobs:
  build:
    # Using the standard 2026 runner
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          # This will automatically pick the best stable version (e.g., Xcode 17.x)
          xcode-version: latest-stable

      - name: Build Archive
        run: |
          xcodebuild archive \
            -project CameraAccess.xcodeproj \
            -scheme CameraAccess \
            -archivePath build/CameraAccess.xcarchive \
            -sdk iphoneos \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            AD_HOC_CODE_SIGNING_ALLOWED=YES

      - name: Create IPA
        run: |
          mkdir -p Payload
          cp -r build/CameraAccess.xcarchive/Products/Applications/CameraAccess.app Payload/
          zip -r TurboMeta.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: TurboMeta-App
          path: TurboMeta.ipa
